/**
 * Magic Mover State Enum
 * Defines the possible states a Magic Mover can be in
 */
export enum MagicMoverState {
    RESTING = 'resting',
    LOADING = 'loading',
    ON_MISSION = 'on-mission',
}

