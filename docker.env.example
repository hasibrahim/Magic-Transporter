# ========================================
# Docker Environment Configuration
# ========================================
# Copy this file to .env.docker and customize the values
# Usage: docker-compose --env-file .env.docker up

# Application Configuration
NODE_ENV=production
PORT=3000
LOG_FORMAT=combined
ORIGIN=*
CREDENTIALS=true

# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=changeme_secure_password
MONGO_DB_NAME=transporters_db
MONGO_PORT=27017

# MongoDB Express (Admin UI)
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=changeme_secure_password
MONGO_EXPRESS_PORT=8081

# MongoDB Connection String (Auto-generated in docker-compose)
# MONGODB_URI will be automatically set in docker-compose.yml
# Format: mongodb://username:password@mongodb:27017/database?authSource=admin

# Optional: Application Specific Variables
# Add any additional environment variables your application needs below
# Example:
# JWT_SECRET=your_jwt_secret_here
# API_KEY=your_api_key_here

